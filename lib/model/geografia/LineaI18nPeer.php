<?php


/**
 * Skeleton subclass for performing query and update operations on the 'linea_i18n' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * 17/07/2013 17:04:34
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    lib.model.crismoe
 */
class LineaI18nPeer extends BaseLineaI18nPeer {
    
    public static function getNomeLinea($id_linea, $lang = '')
    {
        //Obtengo el idioma principal
        if(!$lang)
        {
            $idioma_principal = SfLanguagePeer::getLanguagePrincipal();
            $idioma_principal = $idioma_principal['language'];
        }else{
            $idioma_principal = $lang;
        }
        $c = new Criteria();
        //Eliminamos la columnas de seleccion en caso de que esten definidas
        $c->clearSelectColumns();
        //Se Agregan las Columnas necesarias        
        $c->addSelectColumn(self::NOME_LINEA);
        //Filtros
        $c->add(self::ID,$id_linea, Criteria::EQUAL);
        $c->add(self::LANGUAGE, $idioma_principal, Criteria::EQUAL);        
        $rs = self::doSelectStmt($c);
        //Se recuperan los registros y se genera arreglo
        while($res = $rs->fetch())
        {
            $datoSeccion['nome_linea'] = $res['NOME_LINEA'];
        }
        if (!empty($datoSeccion)){
            return $datoSeccion;
        }else{
            return false;
        }
    }

} // LineaI18nPeer
